using System;
using System.Collections;

namespace Collections
{
    class BitArray_p
    {
        private BitArray bitArray;

        public BitArray_p()
        {
            bitArray = new BitArray(5);
        }
    }
}
